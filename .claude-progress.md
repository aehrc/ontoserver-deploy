# Release Script Implementation Progress

## Status: Task 2 complete, awaiting commit of final fix

### Plan file
`docs/superpowers/plans/2026-03-26-chart-release-script.md`

### Branch
`update-charts`

### Tasks
- [x] Task 1: Script skeleton, argument parsing — committed (6a67044 + fixes)
- [x] Task 2: Safety checks and version reading — code written, one fix applied but NOT YET COMMITTED
- [ ] Task 3: Semver bump logic and next-version resolution
- [ ] Task 4: Tag creation and push
- [ ] Task 5: Update Chart.yaml, commit, and push branch
- [ ] Task 6: Final polish — summary header and README note

### Pending commit (Task 2 fix)
```bash
git add charts/release.sh
git commit -m "fix(charts): strip trailing whitespace from version read; document git clean scope"
```

Changes in that commit:
- `read_current_version()`: adds `; s/[[:space:]]*$//` to sed to strip trailing whitespace/CR
- Adds comment above `check_git_clean()` explaining it checks the whole repo

### Notes for Task 3
- Fix `--next-version` guard to also reject values starting with `--` (reviewer noted this)
- `validate_semver` added in Task 3 will catch bad values anyway, but error message will be clearer with the extra guard

### Next step
Commit the pending fix, then dispatch Task 3 implementer.
